<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/pexelsNavHome"
        android:icon="@drawable/home"
        android:title="@string/home_button"
        app:showAsAction="always" />

    <item
        android:id="@+id/pexelsNavSearch"
        android:icon="@drawable/search"
        android:title="@string/search_button"
        app:showAsAction="always" />

    <item
        android:id="@+id/pexelsNavFavs"
        android:icon="@drawable/like"
        android:title="@string/favs_button"
        app:showAsAction="always" />

    <item
        android:id="@+id/pexelsNavHelp"
        android:icon="@drawable/question"
        android:title="@string/help_button"
        app:showAsAction="always" />

</menu>